# Copy to .env.local and fill in values.

# Lock this frontend to a specific Kowloon instance.
# When set, the server URL field is hidden on the login/register pages.
# Leave blank to enable multi-instance mode (user types the server URL).
VITE_SERVER_URL=
